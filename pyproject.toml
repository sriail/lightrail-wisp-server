[project]
name = "wisp-python-worker"
version = "0.1.0"
description = "Small Wisp v1 TCP-over-WebSocket proxy for Cloudflare Python Workers"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
  "workers-py",
  "workers-runtime-sdk",
]
